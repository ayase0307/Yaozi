# 插圖放這裡

把 PNG（去背、建議寬邊 1200px 以上）丟進這個資料夾，檔名要對得上：

| 檔名 | 用在哪 | 建議比例 |
|---|---|---|
| `hero.png` | 首頁標題右邊的主視覺 | 約 1:1 或 4:3 橫幅 |
| `empty.png` | 還沒有任何專案時的空狀態 | 約 1:1 |

放完要重新建置前端才會進到 `dist`：

```
cd frontend
npm run build
```

檔案沒放也沒關係——找不到圖時該元素會自動隱藏，版面照常。
