# 咬字 Yaozi 規格書

個人專用的影片字幕工具,參考 What'Sub(見 `spec.txt` 逐字稿)但砍掉所有商業功能,
針對單人、本機、免費使用重新設計。

## 一、定位

- **單一使用者**:自己用,不需要登入、會員、付費、額度、分享牆、客服。
- **完全本機**:影片、音訊、辨識、輸出全部在自己電腦上完成,不依賴雲端服務。
- **形式**:本機網頁工具(local web app)——後端在本機跑,瀏覽器開 `localhost` 使用。

## 二、硬體環境(已確認)

| 項目 | 規格 | 影響 |
|---|---|---|
| GPU | NVIDIA GeForce RTX 5070(12GB VRAM,Blackwell 架構) | faster-whisper large-v3 可全速跑;匯出影片可用 NVENC 硬體編碼 |
| RAM | 32GB | 足夠 |
| OS | Windows 11 | — |

> 注意:RTX 5070 是 Blackwell(sm_120),安裝 CUDA 相關套件時需確認
> ctranslate2 / PyTorch 使用支援 Blackwell 的新版本(CUDA 12.8+)。

## 三、核心功能

### 3.1 專案管理
- 首頁列出所有專案(本機資料夾即專案,不需資料庫帳號系統)。
- 新專案:拖放影片/音訊檔即建立。
- 專案內保存:原始媒體路徑、辨識結果、編輯狀態、匯出設定。自動存檔。

### 3.2 語音辨識
- 引擎:**本機 faster-whisper(large-v3)**,啟用單字級時間戳(word timestamps)。
- 流程:拖放檔案 → ffmpeg 抽出音軌 → 辨識 → 顯示進度(耗時/句數)。
- 支援影片與純音訊檔。
- 辨識時把「自訂詞庫」內容餵進 `initial_prompt`,提高人名/品牌名命中率。
- 辨識完成後自動跑:**OpenCC 簡轉繁 + 台灣用語校正**(視頻→影片 等)。

### 3.3 編輯區(完整編輯器)
操作邏輯照 What'Sub 的設計:

| 操作 | 行為 |
|---|---|
| 雙擊字幕 | 進入編輯 |
| `Enter` | 在游標處斷句(拆成兩句,時間自動依字數比例分配) |
| `Backspace`(句首)/ 刪除鍵 | 與上一句合併 |
| `Tab` / `Shift+Tab` | 跳到下一行 / 上一行 |
| 點任意句 | 影片跳到該句時間點 |
| 每句右側 ✕ | 整句刪除(此區 Backspace 語意保留給合併) |

> Alt 快速模式已確定不做:Enter 斷句/Backspace 合併已涵蓋相同能力(2026-08-19 決策)。

- **搜尋**:搜尋字幕全文即時過濾(全部取代可之後再加)。
- **詞庫(已實作,只做清單式)**:「錯誤寫法 → 正確寫法」規則取代,全域存
  `projects/_dictionary.json`;每次辨識完成自動套用,詞庫面板可手動套用到
  目前字幕(走復原系統)、可新增/刪除詞條。initial_prompt 引導與 AI 校正
  掛詞庫列為之後可加的強化,暫不實作(2026-08-19 決策)。

### 3.4 波形區
- 顯示整段音訊波形(建議用 wavesurfer.js + regions plugin)。
- **滑鼠移過即播放**(hover scrubbing),快速定位。
- 每句字幕是一個可拖拉的區塊:拖邊緣調整開始/結束時間。
- 空白處拖選 → 新增字幕;可刪除字幕。
- **Mark 點**:雙擊波形設標記點,拖字幕邊緣靠近時**磁吸**對齊。
- `B` 鍵:在播放頭位置把字幕切開。
- **切點偵測**:掃描影片畫面的剪接點(ffmpeg scene detection),在波形上
  **插旗(藍點)**;拖字幕邊緣時會磁吸到剪接點,讓字幕對齊剪輯點。

### 3.5 LLM 輔助修正(Claude Code CLI 整合)⭐
利用使用者已有的 Claude Code 訂閱,透過 headless 模式(`claude -p`)呼叫,
**不需要另外付 API 費用**。

用途:
1. **錯字/同音字修正**:Whisper 聽錯的字(ㄢㄤ不分、同音異字)整批交給 LLM 校正。
2. **台灣用語校正**:比規則式 OpenCC 更聰明的語境判斷。
3. **智慧重新斷句**:依「每句字數上限、停留時間、語意完整」重新斷句
   (影片裡講的斷句心法,交給 LLM 執行)。
4. **全文一致性**:同一人名/品牌名全文統一寫法(詞庫作為 context 餵入)。

設計原則:
- **結構化輸入輸出**:送出 JSON(編號+文字,不含時間戳),用 `--json-schema`
  強制回傳格式,程式端再驗證編號對得上才接受——LLM 永遠碰不到時間軸。
- **分批處理**:每次送 50~100 句,附前後文;顯示進度。
- **Diff 審閱,不自動套用**:LLM 的修改以逐句 diff 呈現(原文 vs 建議),
  使用者逐句或整批接受/拒絕。接受的修正照常觸發「加入詞庫?」機制。
- **優雅降級**:偵測不到 `claude` CLI 時此功能整塊隱藏,其他功能不受影響。
- **手動觸發**:按按鈕才跑(整集或選取範圍),不做即時逐字呼叫(省訂閱額度)。

**已實測確認(2026-08-19)**:`claude -p` + `--output-format json --json-schema`
可用且回傳 `structured_output` 欄位(schema 強制生效),走訂閱額度。實測數據
帶出的設計要點:

```
claude -p "<提示詞>" --output-format json --json-schema "<schema>"
→ 回傳 JSON:取 .is_error / .subtype / .structured_output
```

- **每次呼叫有固定開銷**:單次呼叫要載入 Claude Code 系統提示
  (實測 cache_creation ~28.8k tokens、單題耗時 ~17 秒)。
  → 批次要大(50~100 句/次),絕不逐句呼叫。
- **1 小時 prompt cache**:連續批次呼叫會命中快取(`ephemeral_1h`),
  同一集的多批修正接連跑最省額度。
- **模型選擇**:預設模型會用使用者 CC 設定的模型(實測是 fable-5,對錯字修正
  太貴太慢)。錯字/斷句這類任務加 `--model sonnet` 即可,更快且省訂閱額度。
- **限制工具**:純文字轉換任務不需要檔案/網路工具,呼叫時用 `--disallowedTools`
  鎖住,避免 agent 亂跑並縮短回應時間。
- **錯誤處理**:檢查 `is_error`、`subtype == "success"`、設 subprocess timeout
  (單批建議 120s),失敗的批次可重試或跳過,不擋住其他批次。

### 3.6 影片預覽區
- 影片播放 + 字幕即時疊加預覽。
- **字幕樣式:寫死一套好看的預設**(粗黑體 + 外框 + 陰影,置底置中),
  **不做**字型選擇、1900 字型庫、字型匯入、樣式牆、卡拉 OK 動畫。
- 保留:字幕垂直位置可微調(一個數值即可)。
- **安全框(已實作,2026-08-19 加回)**:16:9 YouTube、9:16 Shorts/Reels、4:3、
  3:4 四種預設,依影片比例自動選用、可手動切換;紅色斜紋標出平台 UI 遮擋區,
  虛線是內縮安全線,提醒字幕該換行或縮短。偏好記在瀏覽器(每專案)。

### 3.7 匯出
1. **字幕檔**:SRT / VTT → 丟回剪輯軟體。
2. **逐字稿**:純文字 / 含時間戳 Markdown。
3. **燒錄成品影片**:本機 ffmpeg + libass 把字幕燒進影片,
   用 NVENC(h264/hevc)硬體編碼加速。不用瀏覽器內運算(桌機有 GPU,ffmpeg 更快更穩)。

## 四、明確不做(相對 What'Sub)

- 登入 / 會員 / 付費 / 辨識額度 / 推薦機制
- 字型庫、字型匯入、字幕樣式編輯、英雄牆(樣式分享)
- 卡拉 OK 逐字動畫
- 客服機器人
- 翻譯 / 雙語字幕(**暫緩**,架構上預留欄位,之後想做再加)
- 瀏覽器內影片運算(改用本機 ffmpeg)

## 五、建議技術架構(可再討論)

```
┌─ 瀏覽器 (localhost:xxxx) ──────────────────────┐
│  前端:Vite + React(或 Svelte)                │
│  波形:wavesurfer.js(regions/markers plugin)  │
│  影片:<video> + canvas 字幕疊加                │
└──────────────┬─────────────────────────────────┘
               │ HTTP / WebSocket(進度推送)
┌──────────────┴─────────────────────────────────┐
│  後端:Python FastAPI                           │
│  ├─ ffmpeg:抽音軌、切點偵測、燒錄匯出(NVENC)│
│  ├─ faster-whisper:辨識(CUDA, word ts)       │
│  ├─ OpenCC:簡轉繁+台灣用語                    │
│  └─ subprocess → claude -p:LLM 修正            │
│  儲存:專案資料夾 + JSON 檔(不用資料庫)      │
└────────────────────────────────────────────────┘
```

## 六、開發順序建議

1. **MVP**:拖檔案 → 辨識 → 簡轉繁 → 字幕列表編輯(Enter/合併/Tab)→ 匯出 SRT
2. 波形區(顯示、拖拉時間、hover 播放)
3. Claude Code LLM 修正(diff 審閱流程)
4. 詞庫 + 搜尋取代 + Alt 快速模式
5. 切點偵測 + 磁吸 + Mark 點
6. 影片預覽疊加 + 燒錄匯出
7. (之後)翻譯/雙語

## 七、待確認事項

- [ ] 前端框架偏好(React / Svelte / 無所謂)
- [ ] 常見影片長度與檔案格式(影響進度 UX 與預設值)
- [ ] 預設字幕樣式長怎樣(可先做再調)
